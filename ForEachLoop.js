// exploring for Each loop -- >

const num = [1,2,3,4,5]
let sum = 0

num.forEach((i)=>{
    sum += i
})

console.log(sum)

// forEach method consist of a method which will be executed for each and every element of the taken array