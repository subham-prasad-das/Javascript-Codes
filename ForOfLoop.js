// applying for .. of loop on array

let arr = [1,2,3,4,5]

/* for(let i of arr){
     console.log(i)
 }
when traversed using 'i', will get the elements of the array
*/


/*
for(let i of arr){
    console.log(arr[i])
}

when traversed with 'arr[i]' elements will be printed from the first index and go till the length of the arrray, for the last element prints undefined
*/

/*
for (let i in arr){
    console.log(i)
}
when traversed with 'i', the property(index) of the array will be printed 
*/

/*
for (let i in arr){
    console.log(arr[i]);
}
when traversed with 'arr[i]', elements of the array will be printed
*/