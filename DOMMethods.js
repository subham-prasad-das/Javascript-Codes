// let a = document.getElementsByTagName("span")[0].innerHTML = "hellpp";
// console.log(a);



let a = document.getElementsByTagName("div")[0]

let d = document.createElement("div"); // made a new div in the document
d.innerHTML = "<div>Hello</div>"; // given some inner text to the div we created 

// a.append(d); // makes the value of d come after value of a as a same entity.

// a.prepend(d); // makes the value of d come before value of a as a same entity.

// a.before(d); // d come to effect just before a the body tag as a separate entity.

// a.after(d); // d come to effect just after a the body tag as a separate entity.